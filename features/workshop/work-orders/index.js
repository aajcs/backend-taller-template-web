/**
 * Work Orders Module Index
 * Punto de entrada principal del módulo de órdenes de trabajo
 * Exporta el router con todas las rutas del módulo Workshop - Work Orders
 */

const router = require("./routes");

module.exports = router;
