/**
 * Billing Module Index
 * Punto de entrada principal del módulo de facturación
 * Exporta el router con todas las rutas del módulo Workshop - Billing
 */

const router = require("./routes");

module.exports = router;
