/**
 * Vehicles Models Index
 * Exporta todos los modelos relacionados con vehículos
 */

const Vehicle = require("./vehicle.model");
const VehicleBrand = require("./vehicleBrand.model");
const VehicleModel = require("./vehicleModel.model");

module.exports = {
  Vehicle,
  VehicleBrand,
  VehicleModel,
};
