/**
 * Vehicles Module Index
 * Punto de entrada principal del módulo de vehículos
 * Exporta el router con todas las rutas del módulo CRM Vehicles
 */

const router = require("./routes");

module.exports = router;
