/**
 * Customers Controllers Index
 * Exporta todos los controladores del módulo customers
 */

const customerController = require("./customer.controller");

module.exports = {
  customerController,
};
