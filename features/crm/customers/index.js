/**
 * Customers Module Index
 * Punto de entrada principal del módulo de clientes
 * Exporta el router con todas las rutas del módulo CRM Customers
 */

const router = require("./routes");

module.exports = router;
